{
    // let x : any;
    // x = 5;
    // x = 'Ankit'
    
    // console.log(x.toUpperCase()) 

    // let value: unknown = 42;
    // value = "Hello"; 

    
    // if(value instanceof String){
    //     console.log(value.toUpperCase());
    // }

    // function throwError(message: string): never {
    //      throw new Error(message);                   
    // }

    // throwError('ABC')
    // console.log('hi');

    // async function fetchData(url: string): Promise<any> {
    //     try {
    //         const response = await fetch(url);

    //         if (!response.ok) {
    //         throw new Error(`API call failed with status: ${response.status}`);
    //         }

    //         const data = await response.json();
    //         return data;
    //     } catch (error) {
    //         console.error('Error occurred:', error);
    //         throwError(); // Function will never return normally after this                        
    //     }
    // }

    // // Function that throws an error and has a return type of `never`
    // function throwError(): never {
    //     throw new Error('Something went wrong during the API call!');
    // }

    // let msg = await fetchData('https://jsonplaceholder.typicode.co/posts');
    // console.log(msg);
  
    
}